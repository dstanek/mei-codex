What do these commands do?

1. `kubectl run myboot --image=quay.io/burrsutter/myboot:v1 --port=8080`
2. `kubectl expose deployment --port=8080 mybook --type=LoadBalancer`
3. `kubectl scale deployment myboot --replicas=3`
4. `kubectl set image deployment myboot myboot=quay.io/burrsutter/myboot:v2`