1. Create a venv:
```shell
$ uv venv
```
2. Install spartan
	```shell
$ uv init --bare
	```
3. Install requirements (fixme)
	```shell
$ uv pip install -r ../spartan/requirements.txt
	```
4. Copy `nbgrader_conf.py` from another project