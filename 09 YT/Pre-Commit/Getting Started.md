# pre-commit: Getting Started

## Outline

- What problem pre-commit solves (brief, expand on the Short)
- Installation: `pip install pre-commit` / `brew install pre-commit`
- Anatomy of `.pre-commit-config.yaml`: `repos`, `rev`, `hooks`
- Adding your first hooks: `trailing-whitespace`, `end-of-file-fixer`, a linter
- Installing the git hook: `pre-commit install`
- Running manually against all files: `pre-commit run --all-files`
- What happens when a hook fails vs. auto-fixes and re-stages
- Where to find more hooks: pre-commit.com hooks directory

## Script
